# ===== requirements.txt =====
# Core framework
streamlit>=1.39.0

# Enhanced PDF processing with multimodal support
unstructured[all-docs]>=0.11.0    # Comprehensive PDF processing
pillow>=10.4.0                    # Image processing
lxml>=4.9.0                       # XML parsing for unstructured

# Core AI libraries
langchain>=0.3.0
langchain-community>=0.3.0
langchain-core>=0.3.0
langchain-huggingface>=0.1.0

# LLM and Vision API integrations
langchain-groq>=0.1.0             # Fast text generation
langchain-google-genai>=2.0.0     # Google Gemini vision (preferred)
langchain-openai>=0.2.0           # OpenAI GPT-4V fallback
groq>=0.9.0

# Local vector processing
faiss-cpu>=1.8.0                  # Vector database
sentence-transformers>=3.0.0      # Embeddings

# Core ML libraries  
transformers>=4.45.0
torch>=2.1.0
torchvision>=0.16.0
accelerate>=0.34.0

# Data processing
pandas>=2.0.0                     # Table manipulation
numpy>=1.24.0

# System utilities
python-dotenv>=1.0.1             # Environment variables
pathlib>=1.0.0                   # Path handling
uuid>=1.0.0                      # Document ID generation

# Optional: Alternative OCR libraries (kept for compatibility)
# paddleocr>=2.7.0                # Alternative OCR
# opencv-python>=4.8.0            # Computer vision
# easyocr>=1.7.0                  # Alternative OCR

# System dependencies (install via system package manager):
# macOS: brew install poppler tesseract libmagic
# Ubuntu: apt-get install poppler-utils tesseract-ocr libmagic-dev
# Windows: see https://github.com/Unstructured-IO/unstructured for setup guide